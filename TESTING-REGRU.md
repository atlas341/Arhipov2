# Чек-лист тестирования формы после установки на Reg.ru

Полный сценарий проверки от заливки файлов до боевого режима. Идёшь по пунктам сверху вниз, проверяешь каждую галочку.

---

## Часть 1. Подготовка перед заливкой

### ☐ 1.1. Открой config.php в любом текстовом редакторе

Найди и поправь под себя:

```php
'to'        => 'info@tdarhipov.ru',     // ← куда приходят заявки (твоя почта)
'from'      => 'noreply@tdarhipov.ru',  // ← обратный адрес письма
'from_name' => 'Сайт ТД Архипов',       // ← имя отправителя в почте
'use_smtp'  => false,                   // ← оставь false для первого запуска
```

**Важно**: адрес в `from` должен быть на твоём домене (`@tdarhipov.ru`), иначе письма улетят в спам.

### ☐ 1.2. Сохрани config.php

---

## Часть 2. Заливка на хостинг

### ☐ 2.1. Открой файловый менеджер в панели Reg.ru

Зайди в раздел «Хостинг» → твой тариф → «Файловый менеджер».

### ☐ 2.2. Перейди в `/public_html/` (или `/www/` — название зависит от тарифа)

Это **корневая папка сайта** — то, что увидит браузер по адресу `tdarhipov.ru`.

### ☐ 2.3. Удали всё, что там было (если что-то лежит)

Если это новый хостинг — обычно только `index.html`-заглушка.

### ☐ 2.4. Загрузи **весь архив целиком** через «Загрузить файл» → «Распаковать»

После распаковки структура должна быть:
```
public_html/
├── index.html
├── products.html
├── privacy.html
├── send.php
├── config.php
├── .htaccess
├── favicon.ico
├── favicon-32.png
├── apple-touch-icon.png
└── assets/
    ├── css/
    ├── js/
    ├── data/
    ├── img/
    └── svg/
```

### ☐ 2.5. Проверь права (chmod)

- `send.php`, `config.php`, `.htaccess` → **644** (rw-r--r--)
- Папки → **755** (rwxr-xr-x)

Обычно ставится автоматом, но если форма не работает — это первое что проверять.

---

## Часть 3. Настройка почты на Reg.ru

Это нужно потому что выбран режим `use_smtp = false` — письма уходят через локальный sendmail, который требует чтобы домен был **на этом же хостинге**.

### ☐ 3.1. Создай почтовый ящик `info@tdarhipov.ru`

В панели Reg.ru: «Почта» → «Создать ящик» → `info@tdarhipov.ru`. Пароль придумай и запомни — пригодится для проверки.

### ☐ 3.2. (опционально) Создай ящик `noreply@tdarhipov.ru`

Это адрес-отправитель. Письма приходят на info, отправляются с noreply. Чисто эстетически правильнее.

### ☐ 3.3. Убедись, что MX-запись домена указывает на Reg.ru

Если домен куплен у Reg.ru — обычно настроено. Если нет — в DNS должны быть прописаны MX-серверы Reg.ru (они подскажут в саппорте).

---

## Часть 4. Открой сайт и проверь визуально

### ☐ 4.1. Открой `https://tdarhipov.ru` в браузере

**Что должно быть:**
- Прогрузился прелоадер
- Видна шапка с круглым логотипом
- Иконка вкладки браузера — твой круглый логотип (favicon)
- Видна главная страница с фотом фуры

### ☐ 4.2. Нажми `Ctrl+Shift+R` (жёсткое обновление)

На случай если что-то закешировалось от старого деплоя.

### ☐ 4.3. Проверь все три страницы

- `tdarhipov.ru` — главная
- `tdarhipov.ru/products.html` — продукция
- `tdarhipov.ru/privacy.html` — политика

Везде должны быть шапка с логотипом, единая навигация, фавикон во вкладке.

---

## Часть 5. Тест валидации формы

Прокрути вниз до контактной формы. Прогони следующие сценарии:

### ☐ 5.1. Тест «пустая форма»
- Не заполняй ничего
- Нажми «Отправить заявку»
- **Ожидание**: браузер сам подсветит первое незаполненное поле красным и попросит заполнить

### ☐ 5.2. Тест «без галочки»
- Заполни все поля корректно
- Галочку согласия **не ставь**
- Нажми «Отправить заявку»
- **Ожидание**: браузер подсветит чекбокс и не отправит

### ☐ 5.3. Тест «короткий ИНН»
- ИНН: введи `12345` (5 цифр)
- Поставь галочку, попробуй отправить
- **Ожидание**: браузер ругается «ИНН должен содержать 10 или 12 цифр»

### ☐ 5.4. Тест «корректный ИНН-юрлица»
- ИНН: `7707083893` (это реальный ИНН Сбербанка — пригодится дальше для теста ссылок)
- Имя: «Тест Тестов»
- Телефон: твой реальный (на всякий случай)
- Что нужно: «Тест»
- Куда: «Тест»
- Поставь галочку
- Нажми «Отправить заявку»

**Ожидание**: появляется зелёная плашка «Спасибо! Заявка принята.»

---

## Часть 6. Проверка прихода письма

### ☐ 6.1. Открой почту `info@tdarhipov.ru`

Можно через веб-интерфейс Reg.ru (panel.reg.ru → Почта) или через любой почтовый клиент.

### ☐ 6.2. Должно прийти письмо в течение 10–60 секунд

**Тема**: «Заявка с сайта — Тест Тестов (Тест)»

**Содержимое**:
- Тёмная шапка с акцентом «НОВАЯ ЗАЯВКА» и именем
- Таблица всех полей формы
- **Жёлтый блок «Проверка контрагента по ИНН 7707083893»** с шестью кнопками-ссылками

### ☐ 6.3. Письмо не пришло за 60 секунд → ищи в спаме

Если в папке «Спам» — это значит SPF/DKIM не настроены. Решение в части 9 ниже.

### ☐ 6.4. Письмо не пришло вообще, даже в спам → смотри `form-log.txt`

Через файловый менеджер открой `public_html/form-log.txt`. Там строка вида:
```
[03.06.2026 14:32] Тест Тестов | +7... | Тест | inn=7707083893 | ok=NO | mail() вернула false
```

`ok=NO` означает что отправка провалилась. Текст после — причина. Самые частые:
- `mail() вернула false` → почта на хостинге не настроена. Создай ящик заново или переходи на SMTP-режим (часть 8 ниже).
- `Connection refused` / `Connection timeout` → SMTP-сервер не отвечает, проверь host/port в config.

---

## Часть 7. Тест ссылок проверки ИНН

### ☐ 7.1. В пришедшем письме нажми кнопку **«🔍 ФНС — ЕГРЮЛ/ЕГРИП»**

**Ожидание**: открывается официальный реестр ФНС, в нём отображается карточка ПАО «Сбербанк России» с полными реквизитами.

### ☐ 7.2. Нажми **«🎯 Контур.Фокус»**

**Ожидание**: открывается focus.kontur.ru с карточкой Сбербанка. Виден статус «действующая», ОКВЭД, рейтинг рисков.

### ☐ 7.3. Нажми **«👮 ФССП»**

**Ожидание**: открывается сайт ФССП (форма поиска). Здесь нужно вручную ввести ИНН в поле — это сайт без поддержки прямых ссылок, увы. Но менеджеру это 5 секунд работы.

### ☐ 7.4. Нажми остальные кнопки — проверь что открываются нужные сайты

- 📊 Прозрачный бизнес ФНС
- 💰 Финансовая отчётность ФНС
- ⚖️ Арбитражные дела (kad.arbitr.ru)

---

## Часть 8. (Если нужно) Переход на SMTP-режим

Это нужно делать **только** если на этапе 6 письма не приходят и в логе `ok=NO mail() вернула false`. Значит локальная отправка на хостинге не работает.

### ☐ 8.1. В почте, с которой будешь отправлять (Яндекс, Mail.ru, Gmail) — создай **пароль приложения**

- **Яндекс**: `id.yandex.ru/security` → «Пароли приложений» → выбрать «Почта», скопировать сгенерированный пароль
- **Mail.ru**: `account.mail.ru/user/2-step-auth/passwords/` → создать новый
- **Gmail**: `myaccount.google.com/apppasswords` (нужна 2-факторка)

### ☐ 8.2. Скачай PHPMailer

Открой `https://github.com/PHPMailer/PHPMailer/releases` → последний релиз → скачай ZIP.

Распакуй, переименуй папку в `phpmailer`, залей в корень сайта. Должно получиться:
```
public_html/phpmailer/src/PHPMailer.php
public_html/phpmailer/src/SMTP.php
public_html/phpmailer/src/Exception.php
```

### ☐ 8.3. Отредактируй config.php

```php
'use_smtp'    => true,
'smtp_host'   => 'smtp.yandex.ru',        // или smtp.mail.ru / smtp.gmail.com
'smtp_port'   => 465,                     // для Gmail — 587
'smtp_user'   => 'info@tdarhipov.ru',     // полный адрес ящика
'smtp_pass'   => 'тот_самый_пароль',      // НЕ обычный пароль, а пароль приложения
'smtp_secure' => 'ssl',                   // для Gmail — 'tls'
```

### ☐ 8.4. Повтори часть 5–6 (отправь тестовую заявку)

---

## Часть 9. Письма падают в спам — настройка SPF/DKIM

Это нужно для того, чтобы Яндекс/Mail.ru/Gmail верили, что письма с твоего домена шлёшь именно ты, а не кто-то от твоего имени.

### ☐ 9.1. Заходишь в DNS-настройки домена

В Reg.ru: «Домены» → твой домен → «DNS-серверы и управление зоной».

### ☐ 9.2. Добавляешь SPF-запись

Тип: **TXT**, хост: пустой (или `@`), значение:
```
v=spf1 include:reg.ru ~all
```

### ☐ 9.3. Включаешь DKIM в панели хостинга

В Reg.ru: «Почта» → твой домен → «DKIM/DMARC» → «Включить».

После включения система сама добавит TXT-запись в DNS.

### ☐ 9.4. Подожди 1–24 часа

DNS-изменения распространяются не мгновенно. Через сутки проверь повторно — письма должны перестать падать в спам.

---

## Часть 10. Тест защиты от спама

### ☐ 10.1. Тест rate-limit

- Заполни форму корректно, отправь
- Сразу же ещё раз — отправь
- И снова, **четвёртый раз подряд за минуту**
- **Ожидание**: появится красная плашка «Слишком много заявок. Подождите минуту.»

### ☐ 10.2. Тест honeypot против ботов

- Открой DevTools (`F12`) → вкладка Elements
- Найди скрытый div `<div style="position:absolute;left:-9999px..."`
- Внутри него поле `<input name="website">`
- Через DevTools напиши в него любое значение
- Отправь форму
- **Ожидание**: фронт показывает «Спасибо», но в `form-log.txt` записи **нет**, на почту ничего не пришло (это специально — бот должен думать, что отправил, но мы его молча отбросили)

---

## Часть 11. Финальная очистка

### ☐ 11.1. Удали `form-log.txt`

Через файловый менеджер. Этот файл будет создан заново при первой реальной заявке.

### ☐ 11.2. Проверь, что `config.php` не открывается напрямую

Открой в браузере `https://tdarhipov.ru/config.php`

**Ожидание**: 403 Forbidden (доступ запрещён). Если открывается обычный текст — `.htaccess` не сработал, проверь что он залился.

### ☐ 11.3. То же для `form-log.txt`

`https://tdarhipov.ru/form-log.txt` → 403 Forbidden.

---

## Шпаргалка: что делать, если что-то не работает

| Симптом | Куда смотреть | Решение |
|---|---|---|
| Нажимаю «Отправить» — ничего не происходит | DevTools → Console | Скорее всего ошибка в JS, пришли скрин в чат |
| Появилась красная плашка «Нет связи с сервером» | DevTools → Network → последний запрос к `send.php` | Смотри код ответа: 500 = ошибка PHP, 404 = файл не залит, 405 = неправильный метод |
| «Отправить» → «Спасибо», но письма нет | `form-log.txt` в корне | Там написано почему `ok=NO` |
| Письма приходят в спам | Часть 9 (SPF/DKIM) | Настроить и подождать сутки |
| Письма от noreply@... вообще не уходят | Возможно, у домена нет такого ящика | Создай его в панели почты Reg.ru |
| 500-я ошибка при открытии сайта | Файл `.htaccess` мог не залиться корректно | Залей ещё раз, проверь синтаксис |

---

## Контакты для саппорта

- Reg.ru саппорт: 8-800-555-3-555, support@reg.ru, есть чат в панели
- Документация Reg.ru по почте: `kb.reg.ru/article/category/mail-hosting/`
- Документация по DNS: `kb.reg.ru/article/category/dns/`
